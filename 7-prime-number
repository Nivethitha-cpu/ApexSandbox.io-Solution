public Boolean isPrime(Integer num) {
    //code here
    if(num>1)
    {
        for(integer count=2;count<num;count++)
       {
        math.mod(num,count)!=0?  return true: return false;
       }
      
    }
    else
    {
        return false;
    }
}