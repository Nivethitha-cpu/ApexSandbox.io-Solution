public Boolean isPrime(Integer num) {
    //code here
    if(num>1)
    {
      for(integer count=2;count<num;count++)
       {
       return  math.mod(num,count)!=0?true:false;
       }
    }
    else
    {
        return false;
    }
}