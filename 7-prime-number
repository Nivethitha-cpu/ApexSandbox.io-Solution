public Boolean isPrime(Integer num) {
    //code here
   
        for(integer count=2;count<num;count++)
       {
         if(math.mod(num,count)==0)
         {
            return false;
         }
        
       }
       return true;
         
       }
      
    
